spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dewminas?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: ssafy
    password: ssafy
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # 개발 초기에는 update, 운영에는 validate 또는 none 추천
      show-sql: true       # 콘솔에 SQL 쿼리 출력
      properties:
        hibernate:
          format_sql: true
      database-platform: org.hibernate.dialect.MySQL8Dialect

